name: Daily Data Update

on:
  schedule:
    - cron: '0 10 * * *'  # Every day at 6am ET (10am UTC)
  workflow_dispatch:  # Allow manual trigger

permissions:
  contents: write

jobs:
  daily-update:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - uses: r-lib/actions/setup-r@v2

      - name: Install system dependencies for R
        run: sudo apt-get update && sudo apt-get install -y libuv1-dev libcurl4-openssl-dev libssl-dev libxml2-dev libfontconfig1-dev libfreetype6-dev libharfbuzz-dev libfribidi-dev libpng-dev libtiff5-dev libjpeg-dev

      - name: Install R packages
        run: |
          Rscript -e '.libPaths(); install.packages(c("tidyverse", "readr", "purrr", "writexl", "stringr"), lib=.libPaths()[1], repos="https://cloud.r-project.org", dependencies=TRUE); library(tidyverse)'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      # ── STATE DATA (daily) ──
      - name: State data update (incremental)
        run: python scrapers/state_daily_update.py

      - name: Check for terminated candidates
        run: python scrapers/check_terminated.py

      # ── FEC OFFICIAL TOTALS (daily — fast, ~30 API calls) ──
      - name: Pull official FEC totals + by-source + expenditures
        env:
          FEC_API_KEY: ${{ secrets.FEC_API_KEY }}
        run: |
          python3 -c "
          import requests, csv, json, time, os
          API_KEY = os.environ.get('FEC_API_KEY', 'DEMO_KEY')
          with open('scrapers/me_candidates_2026.json') as f:
              cands = json.load(f)

          # Load existing data so we never lose candidates on API failure
          prev_official = {}
          prev_exp = {}
          prev_source = {}
          try:
              with open('viz/data/clean/total_contributions_by_federal_candidate.csv') as f:
                  for r in csv.DictReader(f):
                      prev_official[r['candidate_name']] = r
          except: pass
          try:
              with open('viz/data/clean/total_expenditures_by_federal_candidate.csv') as f:
                  for r in csv.DictReader(f):
                      prev_exp[r['candidate_name']] = r
          except: pass
          try:
              with open('viz/data/clean/contributions_by_source_federal.csv') as f:
                  for r in csv.DictReader(f):
                      prev_source.setdefault(r['candidate_name'], []).append(r)
          except: pass

          official = []
          exp = []
          by_source = []
          api_success = set()

          # Dedup by committee_id to prevent duplicate rows
          seen_comms = set()
          for c in cands:
              comm_id = c['committee_id']
              if comm_id == 'NONE': continue
              if comm_id in seen_comms:
                  print(f'Skipping duplicate committee {comm_id} for {c[\"name\"]}')
                  continue
              seen_comms.add(comm_id)
              try:
                  resp = requests.get(f'https://api.open.fec.gov/v1/committee/{comm_id}/totals/', params={'api_key': API_KEY, 'cycle': 2026}, timeout=60)
                  results = resp.json().get('results', [])
                  if results:
                      t = results[0]
                      api_success.add(c['name'])
                      official.append({'candidate_name': c['name'], 'office': c['office'], 'district': c['district'], 'party': c['party'], 'tot_con': t.get('receipts', 0) or 0, 'individual_unitemized': t.get('individual_unitemized_contributions', 0) or 0, 'cash_on_hand': t.get('last_cash_on_hand_end_period', 0) or 0})
                      exp.append({'candidate_name': c['name'], 'office': c['office'], 'district': c['district'], 'party': c['party'], 'tot_exp': t.get('disbursements', 0) or 0})
                      individual = (t.get('individual_itemized_contributions', 0) or 0) + (t.get('individual_unitemized_contributions', 0) or 0)
                      pac = t.get('other_political_committee_contributions', 0) or 0
                      party_c = t.get('political_party_committee_contributions', 0) or 0
                      cand_self = t.get('candidate_contribution', 0) or 0
                      transfers = t.get('transfers_from_other_authorized_committee', 0) or 0
                      loans = t.get('all_loans_received', 0) or t.get('loans', 0) or 0
                      offsets = t.get('offsets_to_operating_expenditures', 0) or 0
                      other_r = t.get('other_receipts', 0) or 0
                      o, d = c['office'], c['district']
                      if individual: by_source.append({'candidate_name': c['name'], 'office': o, 'district': d, 'source_type': 'Individual', 'total': round(individual, 2)})
                      if pac: by_source.append({'candidate_name': c['name'], 'office': o, 'district': d, 'source_type': 'PAC/Committee', 'total': round(pac, 2)})
                      if party_c: by_source.append({'candidate_name': c['name'], 'office': o, 'district': d, 'source_type': 'Party Committee', 'total': round(party_c, 2)})
                      if cand_self: by_source.append({'candidate_name': c['name'], 'office': o, 'district': d, 'source_type': 'Candidate/Self', 'total': round(cand_self, 2)})
                      if transfers: by_source.append({'candidate_name': c['name'], 'office': o, 'district': d, 'source_type': 'Transfers (Own Committees)', 'total': round(transfers, 2)})
                      if loans: by_source.append({'candidate_name': c['name'], 'office': o, 'district': d, 'source_type': 'Loans', 'total': round(loans, 2)})
                      if offsets + other_r > 0: by_source.append({'candidate_name': c['name'], 'office': o, 'district': d, 'source_type': 'Other', 'total': round(offsets + other_r, 2)})
                  else:
                      print(f'No API results for {c[\"name\"]} — keeping previous data')
                  time.sleep(0.5)
              except Exception as e:
                  print(f'Error for {c[\"name\"]}: {e} — keeping previous data')

          # Preserve previous data for candidates where API returned nothing
          for name, row in prev_official.items():
              if name not in api_success:
                  official.append({k: float(v) if k in ('tot_con','individual_unitemized','cash_on_hand') else v for k, v in row.items()})
          for name, row in prev_exp.items():
              if name not in api_success:
                  exp.append({k: float(v) if k == 'tot_exp' else v for k, v in row.items()})
          for name, rows in prev_source.items():
              if name not in api_success:
                  by_source.extend(rows)

          with open('viz/data/clean/total_contributions_by_federal_candidate.csv', 'w', newline='') as f:
              w = csv.DictWriter(f, fieldnames=['candidate_name','office','district','party','tot_con','individual_unitemized','cash_on_hand'])
              w.writeheader()
              w.writerows(sorted(official, key=lambda x: (x['office'], x['district'], -x['tot_con'])))
          with open('viz/data/clean/total_expenditures_by_federal_candidate.csv', 'w', newline='') as f:
              w = csv.DictWriter(f, fieldnames=['candidate_name','office','district','party','tot_exp'])
              w.writeheader()
              w.writerows(sorted(exp, key=lambda x: (x['office'], x['district'], -x['tot_exp'])))
          with open('viz/data/clean/contributions_by_source_federal.csv', 'w', newline='') as f:
              w = csv.DictWriter(f, fieldnames=['candidate_name','office','district','source_type','total'])
              w.writeheader()
              w.writerows(by_source)
          # Verify no candidates were lost
          assert len(official) >= 30, f'ABORT: Only {len(official)} federal candidates in totals — expected at least 30'
          assert len(exp) >= 30, f'ABORT: Only {len(exp)} federal candidates in expenditures — expected at least 30'
          print(f'Updated {len(official)} totals, {len(exp)} expenditures, {len(by_source)} by-source rows')
          "

      # FEC Schedule A/B (donor/payee line items) handled by weekly full scan
      # Daily only updates official totals + by-source above

      # ── VERIFY ──
      - name: Verify data completeness
        run: |
          python3 -c "
          import csv
          with open('data/state_2026/transactions.csv') as f:
              rows = list(csv.DictReader(f))
          urls = [r['filer_url'] for r in rows if r['filer_url']]
          dupes = len(urls) - len(set(urls))
          print(f'State: {len(rows):,} rows, {dupes} duplicates')
          assert dupes == 0, 'State data has duplicates!'

          # Verify no state candidates were lost
          with open('data/candidate_status.csv') as f:
              visible = sum(1 for r in csv.DictReader(f) if r.get('show_on_page') == 'yes')
          print(f'State candidates visible: {visible}')
          assert visible >= 430, f'ABORT: Only {visible} visible state candidates — expected at least 430'
          print('Verification passed')
          "

      # ── AGGREGATE ──
      - name: Run R aggregation scripts
        run: |
          Rscript scripts/clean_state_data.R
          Rscript scripts/clean_federal_data.R

      # ── UPDATE + PUSH ──
      - name: Update footer dates
        run: |
          DATE=$(date +"%B %-d, %Y")
          for f in viz/governor.html viz/statehouse.html; do
            sed -i "s/<strong>Last updated:<\/strong>[^<]*<br>/<strong>Last updated:<\/strong> $DATE. Data is refreshed daily.<br>/" "$f" 2>/dev/null || true
          done
          for f in viz/senate.html viz/house.html; do
            sed -i "s/<strong>Last updated:<\/strong>[^<]*<br>/<strong>Last updated:<\/strong> $DATE. Candidate totals and contribution breakdowns by source are updated daily. Individual donor and expenditure details from FEC Schedule A and B filings are updated weekly.<br>/" "$f" 2>/dev/null || true
          done

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ viz/data/clean/ viz/*.html
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Daily update: $(date +%Y-%m-%d)"
            git push
          fi
